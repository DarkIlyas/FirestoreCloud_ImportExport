
const firebaseConfig = {
   // 1 - Access to the firebase console
   // 2 - Choose the project where you want to have the db imported/exported
   // 3- Project Overview / click on Project settings
   // 4- Look down for Firebase SDK snippet , choose configuration and copy it here 
  };
  // Exports
  module.exports = firebaseConfig;